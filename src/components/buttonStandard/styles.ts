import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignSelf: 'center',
    borderRadius: 20,
  },
  text: {
    textAlign: 'center',
    paddingVertical: 12,
  },
  onPress: {
    opacity: 0.65,
  },
});
